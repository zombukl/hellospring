package helloSpring.controller;import helloSpring.entity.Account;import helloSpring.repository.AccountRepository;import helloSpring.service.AccountService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.web.bind.annotation.*;import java.util.List;@RestControllerpublic class AccountController {    private final AccountService accountService;    @Autowired    public AccountController(AccountService accountService) {        this.accountService = accountService;    }    @GetMapping("/hello")    public String helloSpring() {        return "Hello Spring!";    }    @PostMapping("/accounts")    public Long createAccount(@RequestBody Account account  ) {        return accountService.createAccount(account.getName(), account.getEmail(),                account.getBill());    }    @GetMapping("/accounts/{id}") //Посмотреть про RequestParam    public Account getAccount(@PathVariable Long id) {        return accountService.getAccountById(id);    }    @GetMapping("/accounts")    public List<Account> gatAll() {        return accountService.getAll();    }    @DeleteMapping("/accounts/{id}")    public Account deleteAccount(@PathVariable Long id) {        return accountService.deleteById(id);    }}